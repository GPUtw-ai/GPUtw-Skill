---
name: 助理行為不對
about: 裝了 skill 之後，AI 助理仍然產生錯誤或不安全的程式碼
labels: behaviour
---

**平台**
Claude Code / Codex CLI / Cursor / Copilot / Gemini CLI，版本：

**你的提問**

**助理的回答**
（貼上產生的程式碼或說明）

**應該是什麼**
例：應該用 `GET /instances/{id}/status` 輪詢，而不是 `GET /instances`
